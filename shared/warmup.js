/* =====================================================================
   發言品質新手教程：五個關卡
   ---------------------------------------------------------------------
   為什麼要有這一頁

     這個站有 AI 引導、有老師、有同學，但學生真正卡住的時候，
     打出來的往往是三個字：「我不會」。
     「我不會」不包含任何資訊 —— 不管問誰，對方都只能反問一遍，
     於是學生覺得「問了也沒用」，下次就不問了。

     ★ 這一頁不教程式，教的是「怎麼把卡住的地方講出來」。
       講得出來，AI 才問得到點上，老師才走得過去，同學才接得上話。

   ---------------------------------------------------------------------
   ⚠️ 這一頁完全不呼叫 AI，判定全部在瀏覽器裡

   ★ 為什麼不接 AI（雖然站上已經有了）
     ① 這一頁教的是「規則」。規則要每個人看到的都一樣 ——
        AI 每次講法不同，學生反而搞不清楚標準到底是什麼。
     ② 30 人 × 5 題 ＝ 150 次，等於一整堂課的額度，
        而且會和 Scratch 批改搶同一個專案的配額。
        批改失敗 → 沒有星數 → 依序開放卡住 → 全班過不了關。
     ③ Gemini 的附加條款寫明不得用於「面向未滿 18 歲」的服務。
        引導那一支是老師在旁邊、當場示範用的；
        這一頁是每個學生第一次進站就要跑的，性質不同。

   ---------------------------------------------------------------------
   ★ 判定的第一原則：寧可放過，不可錯殺

     這一頁的每一次「不通過」，學生看到的都是
     「我認真寫了，它還說我不對」。
     發生兩次，他就會開始猜系統想看什麼字，
     然後為了過關而打字 —— 那和我們要教的東西完全相反。

     所以下面每一條規則都只抓**明顯**的情況，門檻設得很鬆：
       · 太短 → 少於 10 個字才算
       · 空話 → 把所有無資訊的字去掉之後幾乎不剩東西才算
       · 抄題 → 連續 12 個字一模一樣才算
       · 一次多件 → 兩個問號以上，或有「還有」又有問號才算
     模稜兩可的一律放行。寫得普通但有內容，就該過。
   ===================================================================== */
(function (global) {
  'use strict';

  var VERSION = '2026-08-07-warmup';

  /* ── 無資訊的字 ───────────────────────────────────
     「我不會」「不知道啦」「都不懂」拆開來就只剩這些。
     ⚠️ 這裡只放「拿掉之後句子就沒東西了」的字。
        像「重複」「轉」「參數」這種一律不放 —— 那些正是我們要他講的。 */
  var VAGUE = '我你他老師同學不會懂知道道沒有都好難很啊啦耶喔嗎呢的了一點都很超級真的完全整個這個那個東西問題怎麼辦啊,，。！!？?、 　\n\t';

  /** 去掉無資訊的字之後，還剩幾個字 */
  function meat(text) {
    var t = String(text == null ? '' : text);
    var n = 0;
    for (var i = 0; i < t.length; i++) {
      if (VAGUE.indexOf(t.charAt(i)) < 0) n++;
    }
    return n;
  }

  function len(text) {
    return String(text == null ? '' : text).replace(/\s/g, '').length;
  }

  /* ── ① 講具體 ─────────────────────────────────────
     ★ 門檻放得很鬆：10 個字、扣掉空話還剩 4 個字。
       「畫出來變成一直線」是 8 個字、實字 6 個 —— 要過。
       「我不會」是 3 個字、實字 0 個 —— 不過。 */
  /* ★ 第 ① 條要檢查的是「有沒有講到一件事」，不是「有沒有打夠字」。
     ⚠️ 2026-08-10 修：原本只看長度和空話密度 ——
        於是「字夠多」就過得了關，而畫面上寫的規則是
        「講你做了什麼、看到什麼」。**規則名實不符**，
        學生看到四條、以為自己四條都做到了。

     ⇒ 三類詞，命中任何一類就算「講到一件事」：
         做了什麼 —— 按、拉、填、改、執行…
         看到什麼 —— 變成、黏、歪、沒有、跑掉…
         想問什麼 —— 想問、要怎麼、是不是、為什麼…

     ⚠️ 收得很寬，而且是「或」不是「且」。
        這一頁的第一原則是**寧可放過，不可錯殺** ——
        誤判一次，學生就開始猜系統想看什麼字，
        而那正好和這一關要教的東西相反。 */
  var DID  = /做|按|拉|放|改|填|試|執行|點|拼|寫|跑|選|加|刪|設|移|畫|建立|打開/;
  var SAW  = /變成|出現|黏|疊|歪|斷|沒有|不動|跑掉|重疊|空白|直線|錯誤|怪|一樣|不一樣|太大|太小|不見|卡住/;
  var ASK  = /想問|要怎麼|怎麼辦|是不是|為什麼|多少|幾個|幾次|對嗎|可以嗎|該用|哪一|哪個/;

  function checkConcrete(text) {
    var t = String(text == null ? '' : text);
    if (len(t) < 10) {
      return { ok: false, id: 'short',
               why: '太短了 —— 對方看完還是不知道你卡在哪。再多寫幾個字，說說你做了什麼、看到什麼。' };
    }
    if (meat(t) < 4) {
      return { ok: false, id: 'vague',
               why: '這句話裡沒有「發生了什麼事」。把你實際做的動作、或畫面上看到的結果寫進去。' };
    }
    if (!(DID.test(t) || SAW.test(t) || ASK.test(t))) {
      return { ok: false, id: 'nofact',
               why: '字數夠了，但看不出「發生了什麼事」。' +
                    '寫一件你做過的動作（我按了…、我把…改成…），' +
                    '或畫面上看到的結果（變成一條線、兩個黏在一起），' +
                    '再不然就直接寫你想問什麼。' };
    }
    return { ok: true };
  }

  /* ── ② 說出卡在哪一步 ─────────────────────────────
     要的是「指得出位置」：第幾步、或某一個步驟的名字。
     ⚠️ 不強迫寫數字 —— 說得出步驟名稱一樣算指得出位置。 */
  function checkStep(text, stepNames) {
    var t = String(text == null ? '' : text);
    if (/第\s*[0-9一二三四五六七八九十]+\s*步|步驟\s*[0-9]/.test(t)) return { ok: true };
    var hit = (stepNames || []).some(function (s) {
      return s && t.indexOf(s) >= 0;
    });
    if (hit) return { ok: true };
    return { ok: false, id: 'nostep',
             why: '還看不出你卡在哪一步。講「第 3 步」，或直接寫出那一步在做什麼 —— ' +
                  '對方才知道要從哪裡接下去，不用從頭問一遍。' };
  }

  /* ── ③ 用自己的話 ─────────────────────────────────
     ★ 為什麼是「連續 12 個字」這麼寬
       學生本來就會用到題目裡的詞（正方形、重複、副程式）——
       那不是抄，那是在講同一件事。
       真正的抄是整句搬過來，所以門檻設在「連續一長串一模一樣」。
       設 6 個字的話，「重複 4 次的地方」這種正常說法會被誤判。 */
  var COPY_RUN = 12;

  /* 比對前先把標點拿掉。
     ★ 為什麼：題目原文是『把「畫正方形」這一段包成…』，
       學生照抄但省略了那對引號，連續字串就被引號切成兩段，
       最長只剩 10 個字 —— 整句照搬卻驗不出來。
       標點不是內容，不該影響「這是不是同一句話」的判斷。 */
  var PUNCT = /[「」『』（）()【】《》，。、；：？！,.;:?!…—－\-~～"'\s]/g;

  function longestRun(a, b) {
    var s = String(a == null ? '' : a).replace(PUNCT, '');
    var q = String(b == null ? '' : b).replace(PUNCT, '');
    var best = 0;
    for (var i = 0; i < s.length; i++) {
      for (var j = i + best + 1; j <= s.length; j++) {
        if (q.indexOf(s.slice(i, j)) < 0) break;
        best = j - i;
      }
    }
    return best;
  }

  function checkOwnWords(text, source) {
    var run = longestRun(text, source);
    if (run >= COPY_RUN) {
      return { ok: false, id: 'copy',
               why: '這一段有 ' + run + ' 個字和題目一模一樣 —— 那是把題目搬過來，' +
                    '對方讀完還是不知道你在想什麼。用你自己的話講一次看看。' };
    }
    return { ok: true };
  }

  /* ── ④ 一次講一件事 ───────────────────────────────
     ★ 只抓明顯的：兩個以上問號，或「還有／另外」再加一個問號。
       一口氣問三件，對方只會回第一件，剩下兩件就沒了。 */
  function checkOneThing(text) {
    var t = String(text == null ? '' : text);
    var qs = (t.match(/[？?]/g) || []).length;
    if (qs >= 2) {
      return { ok: false, id: 'many',
               why: '這裡有 ' + qs + ' 個問題。對方通常只會回第一個，剩下的就沒下文了 —— ' +
                    '先挑最想知道的那一個問。' };
    }
    if (qs >= 1 && /還有|另外|而且|順便|再來|以及/.test(t)) {
      return { ok: false, id: 'many',
               why: '「還有…」後面那件事，先留著。' +
                    '一次講一件，講完再問下一件，兩件都會有答案。' };
    }
    return { ok: true };
  }

  /* ── 逐條檢查（第 5 關的「對照」畫面用）───────────
     ★ checkAll 回的是「過不過」，這一支回的是**四條各自的結果**。
       學生要看到的是「我做到了哪三條、漏了哪一條」，
       而不是一個過或不過。
     ⚠️ 用的是同一批函式 —— 不可以為了畫面另寫一套判定，
        那樣「畫面上打勾」和「實際判定」會慢慢對不起來。 */
  function ruleCheck(text, o) {
    o = o || {};
    return [
      { n: 1, t: '講你做了什麼、看到什麼', ok: checkConcrete(text).ok },
      { n: 2, t: '說出卡在哪一步',
        ok: checkStep(text, o.names || STEP_NAMES_5).ok },
      { n: 3, t: '用自己的話講',
        ok: !o.source || checkOwnWords(text, o.source).ok },
      { n: 4, t: '一次講一件事', ok: checkOneThing(text).ok }
    ];
  }

  /* 綜合檢查（第 5 關用）。
     ★ 順序有意義：先講「沒內容」，再講「指不出位置」，最後才講「太多件」。
       全部一起丟出來，學生不知道要先改哪一個。

     ⚠️ 2026-08-10 修：這一關的標題是「四條規則一起用」，
        但這裡原本只用了三條 —— **第 ② 條（說出卡在哪一步）漏掉了**。
        結果是：只要字夠多、不是純空話、沒有兩個問號，就過得了關。
        「我剛剛做了很多事情然後發現有一個地方好像怪怪的…」這種
        完全沒有具體資訊的句子照樣通過 ——
        而這一關要教的正好是「講得出你在哪裡卡住」。

     ★ 第 ② 條要收得寬一點（stepNames 給得多）：
       這一關的情境是圖形黏在一起，學生可能講「間隔」「距離」「第二個」，
       也可能講「移動」「往右」—— 都算指得出位置。
       ⚠️ 寧可放過，不可錯殺 —— 這一頁的誤判成本比漏抓高得多。 */
  var STEP_NAMES_5 = [
    '下筆', '停筆', '重複', '移動', '右轉', '起點', '轉彎',
    '間隔', '距離', '空隙', '往右', '旁邊', '邊長',
    '第一個', '第二個', '畫完', '畫第'
  ];
  function checkAll(text, o) {
    o = o || {};
    var r = checkConcrete(text);           if (!r.ok) return r;
    r = checkStep(text, o.names || STEP_NAMES_5); if (!r.ok) return r;
    if (o.source) { r = checkOwnWords(text, o.source); if (!r.ok) return r; }
    r = checkOneThing(text);               if (!r.ok) return r;
    return { ok: true };
  }

  /* ═══ 五個關卡 ═══════════════════════════════════
     每一關都是「先看、再選、然後自己寫」：
       看   —— 兩種說法擺在一起，差別自己看得出來
       選   —— 逼他做一次判斷（比讀完就過去印象深）
       寫   —— 真的動手寫一次，規則才變成自己的
     ★ 只讀不寫的話，學生會覺得「我懂了」，然後照樣打「我不會」。 */
  var STEPS = [
    {
      id: 'concrete',
      badge: '🔍',
      title: '第 1 關　「我不會」是問不到答案的',
      lead: '下課前五分鐘，兩個人同時舉手。老師只走得到一邊 —— 你猜他先走去哪一個？',
      compare: [
        { who: '小安', say: '老師，我不會。', bad: true },
        { who: '小宇', say: '老師，我「重複 4 次」做好了，可是角色一直往前走，畫出來變成一條直線。' }
      ],
      pick: {
        prompt: '老師會先走去哪一邊？為什麼？',
        options: [
          { t: '小安 —— 他比較需要幫忙', ok: false,
            msg: '兩個人一樣需要幫忙。差別是老師走到小安旁邊之後，還要再花三分鐘問清楚他在哪一關、做到哪裡、畫面長什麼樣。下課鐘就響了。' },
          { t: '小宇 —— 因為老師還沒走過去就知道要看什麼', ok: true,
            msg: '就是這樣。小宇的那句話裡有「重複 4 次」「一直往前走」「變成一條直線」—— 老師走過去之前，心裡已經有答案了：少了轉彎。' },
          { t: '都一樣，先舉手的先', ok: false,
            msg: '公平，但幫不上忙。老師走到小安旁邊還是得從頭問起 —— 那三分鐘本來可以拿來解決問題。' }
        ]
      },
      write: {
        prompt: '換你。把「老師，我不會」改寫成一句<b>對方看得懂</b>的話。',
        placeholder: '例如：我按了綠旗，角色動了，可是它…',
        hint: '想想這三件事：你做了什麼？你看到什麼？你本來以為會怎樣？',
        check: function (t) { return checkConcrete(t); },
        ok: '這樣就對了 —— 對方不用先問你三個問題，就能開始幫你。'
      },
      rule: '講你<b>做了什麼</b>、<b>看到什麼</b>，不要只講「我不會」。'
    },

    {
      id: 'step',
      badge: '📍',
      title: '第 2 關　說得出卡在哪一步，別人才接得上',
      lead: '「這題好難」和「我卡在第 3 步」，差在哪裡？<br>' +
            '差在後者已經幫對方省下一半的工作 —— 前面那兩步不用再檢查了。',
      steps: ['1. 讓角色移到起點', '2. 下筆', '3. 重複 4 次（移動 30 點、右轉 90 度）',
              '4. 停筆', '5. 移到旁邊，畫下一個'],
      pick: {
        prompt: '假設你照著上面做，結果畫出一條長長的直線。<b>最可能</b>是哪一步出問題？',
        options: [
          { t: '第 2 步：下筆', ok: false,
            msg: '如果沒下筆，畫面上會什麼都沒有 —— 但你看到了一條線，表示筆是下著的。' },
          { t: '第 3 步：重複 4 次裡面', ok: true,
            msg: '對。線是直的，代表「移動」有做、「轉彎」沒做到 —— 問題就在重複的那一段裡面。' },
          { t: '第 5 步：移到旁邊', ok: false,
            msg: '那一步是畫完之後的事。第一個正方形都還沒成形，問題在更前面。' }
        ]
      },
      write: {
        prompt: '把剛才的判斷寫成一句話，讓對方知道<b>哪裡不用檢查</b>、<b>哪裡要看</b>。',
        placeholder: '例如：前兩步我確定沒問題，第 3 步…',
        hint: '寫出步驟的編號，或直接寫那一步在做什麼。',
        check: function (t) {
          return checkStep(t, ['下筆', '停筆', '重複', '移動', '右轉', '起點', '轉彎']);
        },
        ok: '這句話一送出去，對方就直接跳到第 3 步了 —— 前面兩步的時間省下來了。'
      },
      rule: '指得出<b>第幾步</b>，對方就不必從頭找起。'
    },

    {
      id: 'own',
      badge: '🗣️',
      title: '第 3 關　把題目抄一遍，等於什麼都沒說',
      lead: '題目說：<br>' +
            '<span class="src">把「畫正方形」這一段包成一個副程式，' +
            '再用它畫出六個並排的正方形。</span><br>' +
            '三個人回答了。誰真的講出了自己的想法？',
      pick: {
        prompt: '哪一個是「自己的話」？',
        options: [
          { t: '「就是把畫正方形這一段包成一個副程式，再用它畫出六個並排的正方形。」', ok: false,
            msg: '一個字都沒改。看起來有寫，但對方讀完，還是不知道你懂了沒。' },
          { t: '「我知道了，這題不難。」', ok: false,
            msg: '這句話沒有任何內容 —— 換成任何一題都成立的句子，等於沒說。' },
          { t: '「六個正方形長得一樣，那一段就會做六次，所以把它取個名字，用的時候叫名字就好。」', ok: true,
            msg: '這才是自己的話。他講出了「為什麼要包」（因為重複），也講出了「包了之後怎麼用」 —— 這兩件事題目裡都沒寫。' }
        ]
      },
      write: {
        prompt: '換你用自己的話講一次：<b>為什麼</b>要把那一段包起來？',
        placeholder: '例如：因為那一段…',
        hint: '不要重複題目的句子。想想：如果不包起來，會怎樣？',
        source: '把「畫正方形」這一段包成一個副程式，再用它畫出六個並排的正方形。',
        check: function (t, o) {
          var r = checkConcrete(t); if (!r.ok) return r;
          return checkOwnWords(t, o.source);
        },
        ok: '很好 —— 你講的是題目裡沒有的東西，那才叫「你的想法」。'
      },
      rule: '用<b>自己的話</b>講。抄題目看起來有寫，其實沒有提供任何新訊息。'
    },

    {
      id: 'one',
      badge: '1️⃣',
      title: '第 4 關　一次問一件事',
      lead: '有人這樣問：<br>' +
            '<span class="src">副程式要怎麼建立？還有參數是什麼？' +
            '而且我的正方形為什麼會歪掉？</span><br>' +
            '三個問題，你猜對方會回哪一個？',
      pick: {
        prompt: '對方最可能只回哪一個？',
        options: [
          { t: '第一個 —— 副程式怎麼建立', ok: true,
            msg: '幾乎每次都是第一個。人回訊息會從頭開始回，回完第一個就覺得「回過了」，後面兩個就這樣消失了。' },
          { t: '最後一個 —— 因為那個最急', ok: false,
            msg: '你知道那個最急，但對方不知道。訊息裡沒有任何字說它比較急。' },
          { t: '三個都會回', ok: false,
            msg: '偶爾會，但別賭。而且三個一起回，每一個都只能回一兩句，你反而問不清楚。' }
        ]
      },
      write: {
        prompt: '如果只能問<b>一個</b>，你會先問哪一個？把它寫成一句完整的話。',
        placeholder: '例如：我想先問…',
        hint: '只留一件事。「還有」「另外」後面的那些，等這件解決了再問。',
        check: function (t) {
          var r = checkConcrete(t); if (!r.ok) return r;
          return checkOneThing(t);
        },
        ok: '一次一件，每一件都會有答案。三件一起丟，通常只回得到一件。'
      },
      rule: '<b>一次講一件事</b>。剩下的等這件解決了再問，兩件都會有答案。'
    },

    {
      id: 'final',
      badge: '🏅',
      title: '第 5 關　四條規則一起用',
      /* ── 通過之後的「對照環節」──────────────────
         ★ 為什麼通過了還要再做一件事
           前四關都是「看 → 選 → 寫」，第 5 關原本只有「寫」——
           他寫完就走，沒有機會看到「同一件事還有更好的講法」。
         ★ 順序：先選、再對照
           先讓他選出好的那一句（自己做過判斷，印象才留得住），
           再把他自己寫的擺在旁邊 —— 差在哪不必別人講。
         ⚠️ 這一段**不會擋人**。選錯可以再選，對照完直接完成訓練。
            它是回顧，不是第二道門檻。 */
      wrap: {
        prompt: '同一個情況，四個人這樣問。哪一句<b>對方最快知道要看哪裡</b>？',
        options: [
          { t: '老師，我的正方形怪怪的，可以幫我看一下嗎？',
            ok: false,
            msg: '「怪怪的」沒有講出任何一件事 —— 對方走過來還是要從頭問起。' },
          { t: '第一個正方形畫好了，第二個和它黏在一起，我想問畫完之後要往旁邊走多少。',
            ok: true,
            msg: '四條都做到了：講了看到什麼（黏在一起）、指出位置（畫完之後）、' +
                 '用自己的話、而且只問一件事。' },
          { t: '為什麼會黏在一起？重複 4 次是不是寫錯了？還是邊長要改？',
            ok: false,
            msg: '三個問題一起丟，對方通常只回得到第一個 —— 而你最想知道的可能是第三個。' },
          { t: '你想畫六個並排的正方形，第一個畫出來了，但第二個和第一個黏在一起。',
            ok: false,
            msg: '這是把情境抄一遍。對方已經看得到那段文字了，這句話沒有新訊息。' }
        ]
      },
      lead: '最後一關，沒有選擇題。<br><br>' +
            '<b>情境：</b>你想畫六個並排的正方形。' +
            '第一個畫出來了，但第二個和第一個黏在一起，中間沒有空隙。' +
            '你檢查過「重複 4 次」那一段，看起來沒問題。',
      write: {
        prompt: '寫一句話向 AI 助教求助。前面四條規則<b>同時</b>要做到。',
        placeholder: '在這裡寫下你的求助…',
        hint: '① 講具體　② 說出卡在哪一步　③ 用自己的話　④ 一次一件事',
        source: '你想畫六個並排的正方形。第一個畫出來了，但第二個和第一個黏在一起，中間沒有空隙。',
        check: function (t, o) { return checkAll(t, { source: o.source }); },
        ok: '過關 —— 這句話送給任何人，對方都接得下去。'
      },
      rule: '四條一起用，就是一句「問得到答案」的話。'
    }
  ];

  /* 四條規則，做完之後貼在關卡頁上當小抄 */
  var RULES = [
    { icon: '🔍', t: '講你做了什麼、看到什麼', d: '不要只講「我不會」——那句話不包含任何資訊。' },
    { icon: '📍', t: '說出卡在哪一步', d: '對方就不必從頭檢查一遍。' },
    { icon: '🗣️', t: '用自己的話講', d: '抄題目看起來有寫，其實沒有新訊息。' },
    { icon: '1️⃣', t: '一次講一件事', d: '三件一起問，通常只回得到第一件。' }
  ];

  global.WARMUP = {
    VERSION: VERSION,
    STEPS: STEPS,
    RULES: RULES,
    checkConcrete: checkConcrete,
    checkStep: checkStep,
    ruleCheck: ruleCheck,
    _DID: DID, _SAW: SAW, _ASK: ASK,
    _STEP_NAMES_5: STEP_NAMES_5,
    checkOwnWords: checkOwnWords,
    checkOneThing: checkOneThing,
    checkAll: checkAll,
    _meat: meat,
    _longestRun: longestRun,
    _COPY_RUN: COPY_RUN
  };

})(typeof window !== 'undefined' ? window : this);
