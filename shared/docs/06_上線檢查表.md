# 06 · 上線檢查表

> 最後更新：2026-07-29
> 順序不能顛倒。每做完一項就把 `[ ]` 改成 `[x]`。

---

## 目前進度

名冊轉換、設定切換、推送到舊的兩個 repo 都完成了。
之後把整個系統併成單一 repo `course_115`，剩下的是把它推上去並設定 Pages。

> **接近上線時看這裡：§4.6「開學前實測（11501）」** ——
> 那一節把「已經替你驗過的」和「只有你能驗的」分開列。
> ⚠️ 分開列是刻意的：把兩者混在同一張清單裡，最後只會整張都不看。

---

## A. 已完成

- [x] 發佈 `shared/firestore.rules`
- [x] 共用架構重整：`shared/` 集中共用檔
- [x] 併成單一 repo `course_115`，`11501/` `11502/` 為子資料夾，同步機制整套移除
- [x] 章節測驗引擎與兩學期內容資料檔
- [x] 驗證碼改用 `secret` ＋ 寫入試探
- [x] GAS 併成一支部署、加白名單與通行碼
- [x] Colab 後端改匿名登入
- [x] 檔名去掉學期前綴（115 處引用）

---

## B. 待辦（依順序）

### 0. ⚠️ 備課模式：確認完內容就關掉 🔴

| 檔案 | 現值 | 自動失效日 |
|---|---|---|
| `11501/config.js` | `false`（已上鎖）| — |
| `11502/config.js` | `OPEN_ALL_UNITS: true` | `2027-01-25` |

- [x] 確認完上學期十關的內容 → `11501/config.js` 改回 `false`
      （2026-08-11 當天開、當天關 —— **這學期的鎖從頭到尾沒有真的鬆過**）
- [ ] 確認完下學期十關的內容 → `11502/config.js` 改回 `false`

**為什麼開著**：要調（或再看一次）第 7 關，卻得先把前面每一關都排對流程圖、
上傳作品讓 Colab 批改到 2⭐ —— 那不是謹慎，是讓人乾脆不看。

**忘了關會怎樣**：學生第一天就能跳到第 10 關，而依序開放是這幾個模組
唯一的節奏控制。

> ★ **它會自己關。** 過了上表的日期，`GRADING.openAll()` 就回 `false`，
> 依序開放自動回來。
> 「記得開學前關掉」不是一個機制，是一個願望 ——
> 而願望在忙起來的第一週就會失效，那正是最不能失效的一週。
> ⇒ 忘記的成本從「整學期沒有鎖」降成「某一天開始學生要照順序走」。
>
> ★ 開著的時候，每個受影響的頁面都會掛一條**橘色橫幅**（含到期日）。
> 那不是裝飾，是為了讓「忘了關」不可能安靜發生。
>
> ★ 過期之後 `shared/tests/openall.test.js` 會變紅 ——
> 那是提醒你把設定清掉，不是課程壞了（學生端早就自己鎖回來了）。
>
> ⚠️ 日期請改成你自己的開學日。我填的是保守值（寧可早一點鎖回來）。

### 1. 重跑 Colab notebook
- [ ] Colab 上傳或從 GitHub 開啟 `shared/backend.ipynb`，執行步驟 1～4
- [ ] **先**在 Colab Secrets 設好 `FIREBASE_SA`（服務帳戶金鑰整份 JSON）
- [ ] 確認 `/api/health` 的 `version` 是 `2026-08-03-sa`、`features` 含 `sa_auth`
- [ ] 跑 notebook 的**步驟 5**（OCR 自我測試）與**步驟 6**（資料庫存取自我測試）
      步驟 6 會一次回答：用哪個帳號、批改標準讀不讀得到、哪幾關已經設定好
- [ ] 確認步驟 6 印出的「身分」是 **`service_account`**
      （顯示 `anonymous` 代表金鑰沒設好；教師端「狀態檢查」也會標警告）
- [ ] 確認 Firebase Console → Authentication **只啟用 Google**
      （匿名與電子郵件/密碼已於 2026-08-04 停用；師生都只有這一條路）

> ⚠️ 順序：**先確認 `fs_auth_mode` 是 `service_account`，再發布新的安全規則。**
> 規則已把 `{學期}-grader` 與 `{學期}-scratch-submissions` 收成只有老師，
> 後端若還在匿名模式就會寫不進去 —— 而關卡是依序開放的，全班會卡住。

> 規則已經發佈，**舊版後端現在讀不到批改標準**，重跑之前先別讓學生用 Scratch 批改。

### 2. 執行名冊與驗證碼轉換　✅ 已完成（2026-07-29）
- [x] 雙擊 **`開啟轉換工具.bat`**（在 `course_115/` 根目錄）

  > 它會起一個本機伺服器並打開 `http://localhost:8000/migrate.html`。
  > **不要直接雙擊 `migrate.html`** —— `file://` 來源可能被 Firebase 拒絕，
  > `localhost` 預設就在授權網域清單裡。
  >
  > ⚠️ **這一段是歷史紀錄，照著做已經做不了了。**
  > `shared/migrate.html` 與 `開啟轉換工具.bat` 都已在 2026-08-11 刪除
  > （一次性工具，用完就沒有用了；留著反而會有人拿它去跑一次已經完成的轉換）。
  > 要翻出來看：`git log --diff-filter=D -- shared/migrate.html` 再 `git show`。

- [x] 教師登入 → **① 掃描並預覽**
- [x] **把紅色的衝突列逐筆看過**（每列按「用上學期／用下學期」）
- [x] **② 執行轉換** → **③ 驗證結果**，確認全綠

> 舊集合不會被刪除，出事可以退回去。

### 3. 切換名冊來源
- [x] 26 處寫死的 `'{學期}-roster'` 已改為 `CONFIG.COLLECTIONS.ROSTER`
- [x] `11501_thinking.html` 自有的登入表單已移除（它還會寫 `roster.code`，新規則下會被擋）
- [x] **轉換完成後**，把兩支 `config.js` 的 `COLLECTIONS.ROSTER` 改成 `'roster'`（2026-07-29）

> 🔒 `check.py` 的防呆已經放行 —— 兩個 repo 現在都是「✅ 檢查通過，可以推送」。

### 4. 推送單一 repo　← **你在這裡**
- [ ] GitHub Desktop → **File → Add local repository** → 選 `course_115`
- [ ] 按 **Publish repository**，選既有的空 repo `su-yung-sheng/course_115`
- [ ] GitHub → repo → **Settings → Pages** → Source 選分支 `main` / 資料夾 `/ (root)` → Save
- [ ] 等 1～3 分鐘，開 https://su-yung-sheng.github.io/course_115/ 確認總入口會導向

### 4.5 實測（舊版，驗證碼時期）

> ⚠️ 驗證碼那一套已於 2026-08-04 整套移除（Firebase 停用匿名與 email/密碼登入），
> 現在師生都只有「學校 Google 帳號」一條路。下面前三項留作紀錄，實際請看 §4.6。

- [x] ~~學生登入：驗證碼正確／錯誤～~~（驗證碼已移除）
- [x] ~~教師端名冊：驗證碼看得到、改得動、清得掉~~（驗證碼已移除）
- [ ] 學生通關實測：hub 星星有亮、`progress.history` 有寫入
- [ ] 教師端「🩺 狀態檢查」打得開（網址帶 `?term=`）

---

### 4.6 開學前實測（11501）🔴 十分鐘

> 這一節分兩塊：**已經替你驗過的**（從程式碼與資料查得到）、
> 和**只有你能驗的**（要真的登入、要看 Firebase Console、要開 Colab）。
> 分開列是刻意的 —— 把「查過了」和「還沒查」混在同一張清單裡，
> 最後只會整張都不看。

#### ✅ 已經驗過（2026-08-11，`shared/tests/` 全綠）

不用再花時間，除非之後又動到相關的檔案：

| 項目 | 怎麼驗的 | 結果 |
|---|---|---|
| 連結 | `reach.test.js` 從 `index.html` 與兩個 hub 走連結圖 | 走得到 55 支，**沒有孤兒**；hub 四個模組連結都存在 |
| 舊路徑 | 同上 | 沒有任何檔案引用 `course_11501/`、沒有帶學期前綴的舊檔名 |
| 單元資料 | 比對 `config.UNITS` vs `content/flowchart.js` | 十關代號、順序**完全一致** |
| 題庫 | `qbank.test.js` | 141＋281 題；四選項、答案索引都對；**正解明顯較長的 0 題** |
| 題庫來源 | 同上 | hub 連到的頁面確實用共用引擎 ＋ `content/*.js`（2026-08-11 才切換） |
| 章節測驗能不能作答 | `quizengine.test.js` 真的跑一遍 | 選單 → 教材 → 挑戰 → 送出答案，不丟例外、會換下一題 |
| 安全規則語法 | `rules.test.js` | 呼叫到的函式都有定義、括號收得起來、加分欄位學生寫不動 |
| 計分門檻 | `grading.test.js` | 流程圖 2⭐/關、程式 ≥2⭐ 才開下一關、加分各 +1⭐、上限 30／40 |
| 學期鎖 | `semlock.test.js` ＋ 規則 | `beforeSpring()` = `< 2027-02-01` → **現在學生寫得進 11501** |
| 備課模式 | `openall.test.js` | 11501 已上鎖（`OPEN_ALL_UNITS: false`） |

#### ⚠️ 只有你能驗（我看不到）

- [ ] **安全規則真的發布成功**
      Firebase Console → Firestore → 規則 → 看**最後發布時間**比你上次改規則新，
      而且按發布時沒有紅字。
      > ⚠️ 教師端狀態檢查的第 ⑤ 張卡**證明不了**這件事 ——
      > 它只驗「未登入撈不到名冊」，而舊版規則一樣會擋。
      > 2026-08-06～08-11 就是這樣：規則整整五天沒上線，那張卡天天是綠的。

- [ ] **名冊有全班學號**，而且測試帳號 `1410905` 在裡面
      教師端 → 名冊管理。
      > 學號格式必須是 `14` 開頭的 7 碼 —— `guard.js` 只認這個格式，
      > 而學校信箱是由學號直接組出來的（`qfm{學號}@mail.qfm.kh.edu.tw`）。

- [ ] **Colab 後端開著，而且 `fs_auth_mode` 是 `service_account`**
      教師端 → 🩺 狀態檢查 → 第 ① 張卡。
      > ⚠️ 顯示 `anonymous` 的話 AI 批改**寫不進去**，
      > 而關卡是依序開放的 —— 全班會卡在第 1 關。

- [ ] **`SERVER_URL` 還通**（它是 ngrok 網域，會換）
      同樣看第 ① 張卡；連不上會直接說「連不上」。

#### 🎯 真的走一遍（用學生帳號，不是老師帳號）

- [ ] 登入 → 前導教材「Scratch 清單學習機」→ 通過結業測驗
- [ ] 第 1 關：排對流程圖 → 拿到 2⭐
- [ ] 第 1 關：上傳一份 `.sb3` → AI 批改 → 拿到星星
- [ ] 回教師端，確認那筆成績進去了
- [ ] **F12 主控台不該出現 `permission-denied`**
      > 出現的話就是規則沒發布成功，回到上面第一項。

- [ ] **試著繞過依序開放**：網址直接打 `flowchart.html`，
      在還沒過第 1 關的情況下點第 9 關 —— **應該被擋**，
      而且訊息要說得出「還缺什麼」。
      > ★ 這一項特別值得做：那是依序開放唯一真的能被繞過的地方，
      > 而 2026-08-11 剛動過那一段程式碼（備課模式的短路）。

#### 📌 已知沒設定（不影響上課）

- `11501/config.js` 的 `CLASSROOM.CLIENT_ID` 是空的
  → 繳交審核頁**抓不到 Google Classroom 的作業清單**，
  你要自己開 Classroom 看完再回來按給分。
  加分功能本身正常（那一頁設計成「接不上就顯示原因，名單與給分照樣能用」）。
  要接的話得去 Google Cloud 建一組 OAuth 用戶端 ID。

### 5. GAS
- [ ] Apps Script → 專案設定 → 指令碼屬性 → 新增 `UPLOAD_KEY = c115-upload`
- [ ] 打開部署網址，確認 `keyRequired: true`、`rootName` 是你的資料夾
- [ ] 實測運算思維截圖上傳、Scratch 作品上傳

### 6. 收尾（確認一週沒問題後）
- [ ] 刪除舊集合 `11501-roster`、`11502-roster`
- [ ] 刪除 `firestore.rules` 的「D 待淘汰」整區
- [x] 刪除 `shared/migrate.html`　（2026-08-11 刪；一次性工具，而且裡面還寫著 `_shared/` 的舊路徑）
- [x] 舊的 `cyberethics.html` / `social.html` 換成引擎版　（2026-08-11 完成）

      > ⚠️ 這一條拖了太久，代價很具體：
      > `content/*.js` 的題庫**沒有任何學生在讀**，
      > 2026-08-09 修好的 300 題選項長度等於白改
      > （學生看到的那一份：11501 141 題中 65 題、11502 160 題中 80 題
      > 正解明顯較長，「選最長的」就會對）。
      > 現在 `shared/tests/qbank.test.js` 會擋：
      > 任何 HTML 自己內嵌題庫、或 hub 連到的頁面沒用共用引擎，都紅字。

- [x] 刪掉沒有人連得到的 `11501/music.html`、`11502/tasks.html`
      （2026-08-11；兩支在合併**之前**就已經沒人連了，不是合併弄丟的。
      要找回來：`git log --diff-filter=D -- 路徑` 再 `git show`）
- [ ] 刪掉本機的 `course_11501/` `course_11502/` 兩個舊資料夾
      （內容已併入本 repo；歷史留在 GitHub 上的舊 repo）
- [ ] GitHub 上的舊 repo 可設為 Archived，避免日後誤推

---

## C. 尚未處理（不影響上線，但該排進去）

| 項目 | 說明 |
|---|---|
| **下學期開學日** | 兩支 `config.js` 的 `TERM_START` 都還是 `2026-08-31`。**每週評分與出席週次都以它為準，不改會全錯。**（11501 這個值是對的；要改的是 11502 那一支。） |
| **教師端還在讀已廢除的集合** | `11501/teacher.html`、`11502/teacher.html` 仍查 `artifacts/…/quiz_records`（和 `_115_2`），而 `docs/03` 寫著它 2026-07-29 就併進 `{學期}-progress` 的 `history` 了。查不到東西不會報錯，只是那幾個統計會是空的 —— **看起來像「還沒有人做」**。 |
| **`11502_scratch.html` 第 4、6～10 關沒有題目** | 十關裡只有 1、2、3、5 有內容；其餘顯示「內容準備中」。`levelpage.test.js` 用「有 scene＋quiz＋goal」判定哪幾關算上線，所以不會擋提交。 |
| **`2-3-1` 的問題分析只有 1 題有選擇題** | 3 問裡 2 問沒有 `asks`，會退回「寫一句」——而那擋不住把提示貼上來。它也還沒有情境解說。 |
| appId 收斂 | 全站還有 3 組各頁自己的 appId，未換成統一那組。改了會斷 GA 資料流 |
| 運算思維自有集合 | 仍寫 `artifacts/…/progress_sem2`，未併入 `{學期}-progress` |
| `11502_scratch.html` | 仍是骨架，缺左右分欄、自我檢核測驗、AI 批改接線 |
| `11502_flowchart.html` | 缺上學期後來加的 4 筆教學規則（強制閱讀、選件限制、倒數保護、參考說明頁） |
| hub / teacher 引擎化 | 兩者分別有 90% / 88% 重複，可比照章節測驗抽成引擎 |
