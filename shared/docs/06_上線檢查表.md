# 06 · 上線檢查表

> 最後更新：2026-07-29
> 順序不能顛倒。每做完一項就把 `[ ]` 改成 `[x]`。

---

## 目前進度

名冊轉換、設定切換、推送到舊的兩個 repo 都完成了。
之後把整個系統併成單一 repo `course_115`，剩下的是把它推上去並設定 Pages。

---

## A. 已完成

- [x] 發佈 `shared/firestore.rules`
- [x] 共用架構重整：`shared/` 集中共用檔
- [x] 併成單一 repo `course_115`，`11501/` `11502/` 為子資料夾，同步機制整套移除
- [x] 章節測驗引擎與兩學期內容資料檔
- [x] 驗證碼改用 `secret` ＋ 寫入試探
- [x] GAS 併成一支部署、加白名單與通行碼
- [x] Colab 後端改匿名登入
- [x] 檔名去掉學期前綴（115 處引用）

---

## B. 待辦（依順序）

### 1. 重跑 Colab notebook
- [ ] Colab 上傳或從 GitHub 開啟 `shared/backend.ipynb`，執行步驟 1～4
- [ ] **先**在 Colab Secrets 設好 `FIREBASE_SA`（服務帳戶金鑰整份 JSON）
- [ ] 確認 `/api/health` 的 `version` 是 `2026-08-03-sa`、`features` 含 `sa_auth`
- [ ] 確認 `/api/health` 的 `fs_auth_mode` 是 **`service_account`**
      （顯示 `anonymous` 代表金鑰沒設好；教師端「狀態檢查」也會標警告）
- [ ] 確認 Firebase Console → Authentication **只啟用 Google**
      （匿名與電子郵件/密碼已於 2026-08-04 停用；師生都只有這一條路）

> ⚠️ 順序：**先確認 `fs_auth_mode` 是 `service_account`，再發布新的安全規則。**
> 規則已把 `{學期}-grader` 與 `{學期}-scratch-submissions` 收成只有老師，
> 後端若還在匿名模式就會寫不進去 —— 而關卡是依序開放的，全班會卡住。

> 規則已經發佈，**舊版後端現在讀不到批改標準**，重跑之前先別讓學生用 Scratch 批改。

### 2. 執行名冊與驗證碼轉換　✅ 已完成（2026-07-29）
- [x] 雙擊 **`開啟轉換工具.bat`**（在 `course_115/` 根目錄）

  > 它會起一個本機伺服器並打開 `http://localhost:8000/migrate.html`。
  > **不要直接雙擊 `migrate.html`** —— `file://` 來源可能被 Firebase 拒絕，
  > `localhost` 預設就在授權網域清單裡。

- [x] 教師登入 → **① 掃描並預覽**
- [x] **把紅色的衝突列逐筆看過**（每列按「用上學期／用下學期」）
- [x] **② 執行轉換** → **③ 驗證結果**，確認全綠

> 舊集合不會被刪除，出事可以退回去。

### 3. 切換名冊來源
- [x] 26 處寫死的 `'{學期}-roster'` 已改為 `CONFIG.COLLECTIONS.ROSTER`
- [x] `11501_thinking.html` 自有的登入表單已移除（它還會寫 `roster.code`，新規則下會被擋）
- [x] **轉換完成後**，把兩支 `config.js` 的 `COLLECTIONS.ROSTER` 改成 `'roster'`（2026-07-29）

> 🔒 `check.py` 的防呆已經放行 —— 兩個 repo 現在都是「✅ 檢查通過，可以推送」。

### 4. 推送單一 repo　← **你在這裡**
- [ ] GitHub Desktop → **File → Add local repository** → 選 `course_115`
- [ ] 按 **Publish repository**，選既有的空 repo `su-yung-sheng/course_115`
- [ ] GitHub → repo → **Settings → Pages** → Source 選分支 `main` / 資料夾 `/ (root)` → Save
- [ ] 等 1～3 分鐘，開 https://su-yung-sheng.github.io/course_115/ 確認總入口會導向

### 4.5 實測
- [ ] 學生登入：
  - [ ] 已設過驗證碼的：正確碼能進、錯碼顯示「驗證碼錯誤」
  - [ ] 沒設過的：能進入首次設定並自訂成功
- [ ] 教師端名冊：驗證碼看得到、改得動、清得掉
- [ ] 學生通關實測：hub 星星有亮、`progress.history` 有寫入
- [ ] 教師端「🩺 狀態檢查」打得開（網址帶 `?term=`）

### 5. GAS
- [ ] Apps Script → 專案設定 → 指令碼屬性 → 新增 `UPLOAD_KEY = c115-upload`
- [ ] 打開部署網址，確認 `keyRequired: true`、`rootName` 是你的資料夾
- [ ] 實測運算思維截圖上傳、Scratch 作品上傳

### 6. 收尾（確認一週沒問題後）
- [ ] 刪除舊集合 `11501-roster`、`11502-roster`
- [ ] 刪除 `firestore.rules` 的「D 待淘汰」整區
- [ ] 刪除 `shared/migrate.html`
- [ ] 舊的 `cyberethics.html` / `social.html` 換成引擎版
      （`cyberethics_new.html` / `social_new.html`），確認後改名取代
- [ ] 刪掉本機的 `course_11501/` `course_11502/` 兩個舊資料夾
      （內容已併入本 repo；歷史留在 GitHub 上的舊 repo）
- [ ] GitHub 上的舊 repo 可設為 Archived，避免日後誤推

---

## C. 尚未處理（不影響上線，但該排進去）

| 項目 | 說明 |
|---|---|
| **下學期開學日** | 兩支 `config.js` 的 `TERM_START` 都還是 `2026-08-31`。**每週評分與出席週次都以它為準，不改會全錯。** |
| appId 收斂 | 全站還有 3 組各頁自己的 appId，未換成統一那組。改了會斷 GA 資料流 |
| 運算思維自有集合 | 仍寫 `artifacts/…/progress_sem2`，未併入 `{學期}-progress` |
| `11502_scratch.html` | 仍是骨架，缺左右分欄、自我檢核測驗、AI 批改接線 |
| `11502_flowchart.html` | 缺上學期後來加的 4 筆教學規則（強制閱讀、選件限制、倒數保護、參考說明頁） |
| hub / teacher 引擎化 | 兩者分別有 90% / 88% 重複，可比照章節測驗抽成引擎 |
