# 04 · 後端與資料庫

> 三個後端：Firestore、GAS 檔案備份、Colab。**三個都是上下學期共用一份。**
> ⚠️ 但 Colab 那一份是**同一本 notebook 跑在兩台機器上**（批改一台、OCR 一台），
> 見第 3 節。文件裡說「Colab 後端」時，要先確認講的是哪一台。

---

## 1. Firestore

結構維持「前綴分學期」（`{學期}-progress`），沒有改成子集合。理由：

- 安全規則會變複雜（子集合要另寫一層 match，還要處理 collectionGroup）
- 教師端所有查詢都要改成 collectionGroup，成本高
- 前綴分法有個容易被忽略的優點：**學期結束後整包封存或刪除很單純**

集合清單見 [03 資料格式規格](03_資料格式規格.md)。

### 2026-07 的四項整理

| 項目 | 處理 |
|---|---|
| 名冊兩份（同一批學生） | 合併成跨學期共用的 `roster` |
| 闖關紀錄命名不一致且藏在 `artifacts/…` | 併進 `{學期}-progress` 的 `history` |
| `{學期}-grades` 規則有、程式沒用 | 刪除 |
| appId 散了 5 組 | ⚠️ **尚未收斂**，見 [06 上線檢查表](06_上線檢查表.md) |

安全規則的正本是 `shared/firestore.rules`。
**一個 Firebase 專案只有一份規則，發佈時整份覆蓋** ——
不要只貼其中一段，否則另一個學期會被鎖死。

---

## 2. GAS 檔案備份（`shared/filebackup.gs`）

一支部署服務兩學期兩種檔案。

### 資料夾規則

```
<根> / 學期 / 單元 / 班級 / 座號 / 檔名

<根>/11501/thinking/801/05/03.png     運算思維通關截圖
<根>/11501/scratch/801/05/03.sb3      Scratch 作品（AI 批改 2⭐ 以上才上傳）
```

舊版是「截圖一個根、作品一個根」，各自少一層「單元」，
所以必須各開一支 Apps Script 專案、`TERM` 還得寫死 ——
腳本註解自己就寫著「本學期請另外開一個專案部署」，這正是漂移的來源。
**多包一層單元之後，一支部署就夠了。**

### 三道檢查

| 檢查 | 作用 |
|---|---|
| `checkTerm()` | 學期白名單（`ALLOWED_TERMS`），亂傳會被擋下並回報錯誤 |
| `checkKey()` | 請求要帶 `key`，與指令碼屬性的 `UPLOAD_KEY` 比對 |
| `checkSize()` | 單檔上限 15 MB |

前端對應：`config.js` 的 `GAS_UPLOAD_URL` 與 `GAS_UPLOAD_KEY`，**兩學期填同一個值**。
上傳 payload 必須帶 `term` 與 `key`。

### 部署

1. Apps Script 專案貼上 `shared/filebackup.gs`
2. 專案設定 → 指令碼屬性 → 新增 `UPLOAD_KEY`（值同 `config.js` 的 `GAS_UPLOAD_KEY`）
3. 部署 → 網頁應用程式，執行身分「我自己」／存取權「任何人」
4. **用瀏覽器直接打開部署網址**，`doGet` 會列出根資料夾名稱、兩學期的資料夾連結、
   `keyRequired` 是否為 `true` —— 打開看到的路徑對不對，比等學生上傳失敗才發現好
5. 選用：手動執行 `createAllSeatFolders("11501")` 預建座號資料夾

> 改完一定要**重新部署**（管理部署作業 → 編輯 → 版本選「新版本」），
> 用「編輯現有部署」網址才不會變，前端就不用改。

---

## 3. Colab AI 批改後端（`shared/backend.ipynb`）

### 一本 notebook，兩台機器（2026-09-02 起）

`11501_backend.ipynb` 已刪除。原因：兩本寫死同一個 ngrok 靜態網域，
免費版一帳號只有一個，誰後啟動誰佔走。

現在是**一本 notebook 跑在兩台 Colab 上**，兩邊都按「全部執行」：

| | 🎨 Scratch 批改機 | 🧠 運算思維 OCR 機 |
|---|---|---|
| 做什麼 | 讀 `.sb3`、呼叫 Gemini 評分 | 收截圖、PaddleOCR 判關卡 |
| 網域 | `flanking-snort-cyclic`（預設值） | `shuffle-unclaimed-jovial`（設 `NGROK_DOMAIN`） |
| 對應 `config.js` | `SERVER_URL` | `OCR_SERVER_URL` |

> **程式碼完全不必區分。** 這一本會依照「裝了什麼、Secrets 設了什麼」
> 自動決定自己能提供哪些服務 —— 啟動訊息的「── 這一台實際提供的服務 ──」
> 就是它對自己的判斷，**以那幾行為準**，不要憑印象。

三個自動判斷（都在 `_print_roles()` 與 `start_temp_worker()`）：

| 判斷依據 | 結果 |
|---|---|
| 有沒有 `GEMINI_API_KEY_1` | 決定要不要提供 `/api/student/grade` |
| 有沒有**裝** `paddleocr` | 決定要不要提供 `/analyze` |
| 有沒有裝 `paddleocr` ＋ GAS 金鑰 | 決定要不要啟動暫存區工作者 |

> ⚠️ 第三項 2026-09-04 才補上。在那之前只看 GAS 金鑰 ——
> 批改機只要金鑰設一樣就會**每 8 秒去搶同一批圖**：重複抓、做不出結論、
> 兩台一起燒 Apps Script 每天 90 分鐘的額度，而且兩邊都說自己正常。

### 截圖是走雲端暫存區，不是直接打 Colab（2026-09-03 起）

```
學生瀏覽器 ──POST──► GAS「等待驗證/日期/」資料夾
                              │
        Colab OCR 機每 8 秒掃一次，一張一張取來辨識
                              ▼
        通過 → 存進證書區、寫 Firestore、刪掉暫存檔
        沒過 → 移到「未通過/」留存（不是刪，學生要申訴時調得出來）
        沒結論 → 留在原地，下一輪再試
```

三個由此而來的約定：

- **「還在資料夾裡」＝「還沒處理完」**。判斷「我好了沒」的方式是
  「我的檔案從排隊清單消失了」，不必另外做狀態機。
- **上傳不依賴 Colab**。Colab 掛掉、滿載都不影響學生送出 ——
  圖在雲端等著，之後會處理完。學生送出後可以直接關視窗。
- **顯示才依賴 Colab**。進度和結果讀 `/api/queue-list`（記憶體快取），
  Colab 掛掉時看不到進度，但圖是安全的。

> ⚠️ 因此 `/queue` 那組舊的排隊計數**已經沒有意義**：工作者一次一張，
> 佇列深度永遠 0 或 1。2026-09-04 修過一個因此產生的假綠燈 ——
> `status.html` 本來讀 `/queue`，暫存區積 50 張時它照樣顯示
> 「0 人排隊中／佇列正常」。**任何「還有幾張沒處理」的問題，
> 答案在 `/api/queue-list`。**

前端呼叫一律帶 `term`（用 `CONFIG.TERM`，不要寫死）：

| 前端送出 | 後端讀寫 |
|---|---|
| `term=11501` | `11501-grader` / `11501-scratch-submissions` |
| `term=11502` | `11502-grader` / `11502-scratch-submissions` |

沒帶 `term` 時退回 `GRADER_TERM`（預設 `11501`），讓舊頁面照常運作。

### Firestore 存取身分：服務帳戶

演進過程（三代）：

| 版本 | 身分 | 規則被迫開到多鬆 |
|---|---|---|
| 最早 | 未登入 REST（只帶 apiKey） | `if true` —— 全世界都能讀寫 |
| 2026-07-29 | 匿名登入取 `idToken` | `if isSignedIn()` —— 縮小成「有登入的人」，但學生也是 |
| **2026-08-03** | **服務帳戶** | 可以完全不為後端開洞 |

服務帳戶的差別在於它是「後端自己的身分」，而且**不受安全規則限制**。
所以規則可以為學生收到最緊，而 Firebase 的「匿名」登入方式也就能關掉了。

| 函式 | 作用 |
|---|---|
| `_sa_credentials()` | 從 Colab Secrets 的 `FIREBASE_SA` 建憑證；順便擋掉「貼成別的專案」 |
| `_sa_token()` | 服務帳戶 access token（`google-auth` 自動續期） |
| `_fs_token()` | 優先服務帳戶，取不到才退回匿名 |
| `_fs_signin()` / `_fs_refresh()` | 匿名退路（舊機制，保留） |
| `fs_auth_mode()` | 回報目前用哪一種，`/api/health` 會帶出來 |

**設定 `FIREBASE_SA`（只要做一次）**

1. Firebase Console → ⚙️ 專案設定 → **服務帳戶** → 產生新的私密金鑰 → 下載 JSON
2. Colab 左側 **🔑 Secrets** → 新增 `FIREBASE_SA`，把整份 JSON 原封不動貼進去
3. 打開該密鑰的「筆記本存取權」

> ⚠️ 這把金鑰等於資料庫完整權限。**不要**進 GitHub、不要貼進任何前端檔案。
> 放 Colab Secrets 是因為它不會進版本控制。

**沒設定會怎樣**：後端退回匿名登入，並在 Colab 畫面大聲提醒；
`/api/health` 的 `fs_auth_mode` 顯示 `anonymous`，教師端的「狀態檢查」會標成警告。
會退不會停，是因為上課中途壞掉的代價比較高 —— 但只要你在 Console 關掉「匿名」，
這條退路就會真的失效。

### 什麼時候才能關掉 Firebase 的「匿名」登入

✅ **2026-08-04 已完成**。Firebase Console → Authentication → 登入方式，
現在只啟用 **Google** 一項；「匿名」與「電子郵件/密碼」都已停用。

連帶整套移除的：驗證碼登入、`secret`／`session` 集合、名冊的 `hasCode`、
安全規則的 `legacyAllowed()`、以及進度的「動作幅度」防呆
（`starsNotReduced` 等，那是匿名時代認不出本人才需要的替代品）。

⚠️ **現在沒有任何備援登入方式**。學校 Google 一掛，師生都進不去。
   這是 2026-08-04 明確選擇的取捨 —— 學校帳號掛掉時很多事本來也做不了。

### Colab Secrets（11 個，兩台各設各的）

✅ 必填　⭕ 選填但建議　❌ **不要設**

| Secret | 用途 | 🎨 批改機 | 🧠 OCR 機 |
|---|---|---|---|
| `NGROK_TOKEN` | 啟動 ngrok 固定網域 | ✅ | ✅ |
| `FIREBASE_SA` | 服務帳戶金鑰（整份 JSON） | ✅ | ✅ |
| `NGROK_DOMAIN` | 第二台的網域，避免兩台搶同一個 | ❌ | ✅ |
| `GEMINI_API_KEY_1` | AI 批改 | ✅ | ❌ |
| `GEMINI_API_KEY_2` | 第二把金鑰，額度用完自動輪替 | ⭕ | ❌ |
| `GAS_UPLOAD_URL` | 截圖暫存區（**要用「檔案備份」那個部署**） | ❌ | ✅ |
| `GAS_UPLOAD_KEY` | 同上的驗證金鑰 | ❌ | ✅ |
| `CLASS_PASSCODE2` | 本學期班級密碼 | ❌ | ✅ |
| `CLASS_PASSCODE` | 上學期班級密碼 | ❌ | ⭕ |
| `SKIP_OCR` | 跳過步驟 1b 的安裝（省 1～3 分鐘） | ⭕ 填 `1` | ❌ |
| `NGROK_API_KEY` | 舊連線卡住時自動清除（見下方） | ⭕ | ⭕ |

**沒設會怎樣**：`NGROK_TOKEN` 直接 `raise`，開不起來；其餘都是**安靜地少做一件事**，
所以一定要看啟動訊息的「── 這一台實際提供的服務 ──」對答案。

三個最容易踩的坑：

1. **每個 Secret 右邊都有「筆記本存取權」開關。** 有值但沒開，
   `userdata.get()` 會丟例外被 `except` 吞掉，**效果等於沒設**。
   `SKIP_OCR` 最明顯：加了卻還是照裝，就是這個。
2. **`NGROK_DOMAIN` 設在 OCR 那台**，不是批改機。程式碼的預設值就是
   批改機的網域，剛好對上 `config.js` 的 `SERVER_URL`。
3. **`GAS_UPLOAD_URL` 要用「檔案備份」那個部署。** 指到 Classroom 或
   AI 引導的部署時，症狀是 `不認得的上傳種類：temp_list`。

> 匿名退路用的是 `config.js` 裡那把公開的 Firebase `apiKey`，不需要 Secret；
> 服務帳戶則需要 `FIREBASE_SA`。
> `CLASS_PASSCODE` 對應的 `CLASS_PASS` 明寫在 `config.js`，學生看得到，別當成真正的防護。

### 設好之後，兩台的啟動訊息長這樣

```
🎨 批改機                        🧠 OCR 機
✅ Scratch AI 批改               ❌ Scratch AI 批改 —— 沒有 GEMINI_API_KEY_1
❌ 運算思維 OCR                  ✅ 運算思維 OCR
⏭️ 跳過辨識引擎預載              ✅ 辨識引擎已預先載入
ℹ️ 沒設 GAS…不啟動工作者         ✅ 暫存區工作者已啟動（每 8 秒掃一次）
```

批改機那三行 `❌ ⏭️ ℹ️` **是對的，不是警告**。

> 匿名退路用的是 `config.js` 裡那把公開的 Firebase `apiKey`，不需要 Secret；
> 服務帳戶則需要 `FIREBASE_SA`。
> `CLASS_PASSCODE` 對應的 `CLASS_PASS` 明寫在 `config.js`，學生看得到，別當成真正的防護。

### 怎麼確認 Colab 跑的是最新版

後端 `/api/health` 回報 `features` 清單，前端比對 `config.js` 的 `BACKEND_FEATURES`。

- 新增端點時：後端 `features` 加一個名稱 → 兩支 `config.js` 的 `BACKEND_FEATURES` 加同一個
- `SERVER_VERSION` 純顯示，前端不做判斷
- 少了任一項，教師端會主動提示你去 Colab 重跑

**上課前開教師端工具列的「🩺 狀態檢查」**（`shared/status.html`），一次確認五件事：
後端版本、Gemini 金鑰、排隊機制、兩學期批改標準是否分開、Firestore 規則是否生效。

### 換 notebook 的正確做法

Colab 上跑的多半是雲端硬碟裡的**副本**。只重跑舊副本，改動不會生效。

- 換版：Colab → 檔案 → 上傳筆記本 → 選 `shared/backend.ipynb`
- 或更好：Colab → 檔案 → 開啟筆記本 → GitHub → 選 repo 的 `shared/backend.ipynb`，
  這樣永遠跑到最新版，不會有副本走鐘的問題

### ngrok「被佔用」的兩種錯，處理方式不同

| 錯誤 | 意思 | 自動處理 |
|---|---|---|
| `ERR_NGROK_334` | 靜態網域已經在線上 | 停掉佔用**該網域**的那條連線 |
| `ERR_NGROK_108` | 帳號同時只能有一條 agent 連線 | 停掉帳號底下**所有**連線 |

兩者都需要 `NGROK_API_KEY`：`ngrok.kill()` 只殺得掉**本 runtime** 的 agent，
佔用的若是另一個 Colab 分頁、或斷線後雲端還沒回收的舊 runtime，本機怎麼殺都沒用。

> ⚠️ `NGROK_API_KEY` 與 `NGROK_TOKEN` **不是同一個東西**：
> - `NGROK_TOKEN` → agent 用的 authtoken（Your Authtoken 頁）
> - `NGROK_API_KEY` → 雲端 API 用的（[API Keys 頁](https://dashboard.ngrok.com/api-keys)）
>
> 貼錯的話 API 會回 401，畫面會印出「ngrok API 拒絕這把金鑰」。
> 另外記得打開該密鑰的**筆記本存取權**，否則 Colab 讀不到，等於沒設。

臨時解法：https://dashboard.ngrok.com/agents 把舊 agent 按 Stop。
